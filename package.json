{
  "name": "website-fullstack",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "concurrently -n \"client,server\" -c \"cyan,green\" \"npm run start --prefix client\" \"npm run start --prefix server\"",
    "build": "npm run build --prefix client",
    "start:client": "npm run start --prefix client",
    "start:server": "npm run start --prefix server"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "dependencies": {
    "ignore": "^7.0.5",
    "react-syntax-highlighter": "^15.6.1"
  }
}
